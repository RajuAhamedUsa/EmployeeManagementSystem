import { SearchEmpPipe } from './search-emp.pipe';

describe('SearchEmpPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmpPipe();
    expect(pipe).toBeTruthy();
  });
});
